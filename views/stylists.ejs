<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stylist - Lyn Salon</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .stylists-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .stylists-header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .stylists-header h1 {
            color: #333;
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .stylists-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }
        
        .stylist-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        
        .stylist-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }
        
        .stylist-image {
            width: 100%;
            height: 250px;
            object-fit: cover;
            background: #f8f9fa;
        }
        
        .stylist-info {
            padding: 20px;
        }
        
        .stylist-name {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }
        
        .stylist-specialization {
            background: #007bff;
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9rem;
            display: inline-block;
            margin-bottom: 15px;
        }
        
        .stylist-experience {
            background: #f8f9fa;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9rem;
            color: #666;
            display: inline-block;
            margin-bottom: 15px;
            margin-left: 10px;
        }
        
        .stylist-description {
            color: #666;
            line-height: 1.6;
            margin-bottom: 20px;
        }
        
        .stylist-actions {
            display: flex;
            gap: 10px;
        }
        
        .btn-book-stylist {
            flex: 1;
            padding: 10px;
            background: #007bff;
            color: white;
            text-decoration: none;
            text-align: center;
            border-radius: 5px;
            transition: background 0.3s;
        }
        
        .btn-book-stylist:hover {
            background: #0056b3;
        }
        
        .no-stylists {
            text-align: center;
            padding: 50px;
            color: #666;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .stylists-grid {
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 20px;
            }
            
            .stylists-header h1 {
                font-size: 2rem;
            }
        }
        
        @media (max-width: 480px) {
            .stylists-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="stylists-container">
        <div class="stylists-header">
            <h1>Stylist của chúng tôi</h1>
            <p>Đội ngũ stylist chuyên nghiệp với nhiều năm kinh nghiệm</p>
        </div>
        
        <% if (stylists && stylists.length > 0) { %>
            <div class="stylists-grid">
                <% stylists.forEach(function(stylist) { %>
                    <div class="stylist-card">
                        <img src="<%= stylist.image_url || '/images/stylist.jpg' %>" alt="<%= stylist.name %>" class="stylist-image" onerror="this.src='/images/stylist.jpg'">
                        <div class="stylist-info">
                            <h3 class="stylist-name"><%= stylist.name %></h3>
                            <div class="stylist-specialization">
                                <%= stylist.specialization %>
                            </div>
                            <div class="stylist-experience">
                                <%= stylist.experience %> năm kinh nghiệm
                            </div>
                            <p class="stylist-description">
                                <%= stylist.description %>
                            </p>
                            <div class="stylist-actions">
                                <a href="/book-appointment?stylist=<%= stylist.id %>" class="btn-book-stylist">Đặt lịch</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="no-stylists">
                <h3>Không có stylist nào</h3>
                <p>Vui lòng liên hệ với chúng tôi để biết thêm thông tin.</p>
            </div>
        <% } %>
    </div>
    
    <%- include('partials/footer') %>
</body>
</html> 