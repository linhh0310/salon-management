<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Lyn Salon</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .product-detail-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .product-detail {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-top: 30px;
        }
        
        .product-image-section {
            text-align: center;
        }
        
        .product-main-image {
            width: 100%;
            max-width: 600px;
            height: 600px;
            object-fit: cover;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .product-info-section {
            padding: 20px;
        }
        
        .product-title {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
            line-height: 1.3;
        }
        
        .product-category {
            background: #e9ecef;
            color: #495057;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            display: inline-block;
            margin-bottom: 20px;
        }
        
        .product-price {
            font-size: 2.5rem;
            font-weight: bold;
            color: #e74c3c;
            margin-bottom: 20px;
        }
        
        .product-quantity-info {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        
        .quantity-label {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .quantity-value {
            font-size: 1.2rem;
            color: #666;
        }
        
        .quantity-status {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 15px;
            font-size: 0.9rem;
            margin-left: 10px;
        }
        
        .quantity-in-stock {
            background: #d4edda;
            color: #155724;
        }
        
        .quantity-low {
            background: #fff3cd;
            color: #856404;
        }
        
        .quantity-out {
            background: #f8d7da;
            color: #721c24;
        }
        
        .product-description {
            color: #666;
            line-height: 1.6;
            margin-bottom: 25px;
        }
        
        .product-features {
            margin-bottom: 30px;
        }
        
        .features-title {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }
        
        .features-list {
            list-style: none;
            padding: 0;
        }
        
        .features-list li {
            padding: 8px 0;
            color: #666;
            position: relative;
            padding-left: 25px;
            font-size: 1rem;
        }
        
        .features-list li:before {
            content: "✓";
            color: #28a745;
            font-weight: bold;
            font-size: 1.2rem;
            position: absolute;
            left: 0;
        }
        
        .product-actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }
        
        .btn-buy-now {
            flex: 2;
            padding: 15px 30px;
            background: #28a745;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s;
        }
        
        .btn-buy-now:hover {
            background: #218838;
        }
        
        .btn-buy-now:disabled {
            background: #6c757d;
            cursor: not-allowed;
        }
        
        .btn-back {
            flex: 1;
            padding: 15px 20px;
            background: #6c757d;
            color: white;
            text-decoration: none;
            text-align: center;
            border-radius: 8px;
            font-size: 1.1rem;
            transition: background 0.3s;
        }
        
        .btn-back:hover {
            background: #5a6268;
        }
        
        .quantity-input {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
        }
        
        .quantity-input label {
            font-weight: bold;
            color: #333;
        }
        
        .quantity-input input {
            width: 80px;
            padding: 8px;
            border: 2px solid #ddd;
            border-radius: 5px;
            text-align: center;
            font-size: 1rem;
        }
        
        .quantity-input input:focus {
            outline: none;
            border-color: #007bff;
        }
        
        .breadcrumb {
            margin-bottom: 20px;
        }
        
        .breadcrumb a {
            color: #007bff;
            text-decoration: none;
        }
        
        .breadcrumb a:hover {
            text-decoration: underline;
        }
        
        .breadcrumb span {
            color: #666;
        }
        
        @media (max-width: 768px) {
            .product-detail {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .product-actions {
                flex-direction: column;
            }
            
            .product-title {
                font-size: 1.5rem;
            }
            
            .product-price {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="product-detail-container">
        <div class="breadcrumb">
            <a href="/">Trang chủ</a> > 
            <a href="/products">Sản phẩm</a> > 
            <span><%= product.name %></span>
        </div>
        
        <div class="product-detail">
            <div class="product-image-section">
                <img src="<%= product.image_url %>" alt="<%= product.name %>" class="product-main-image" onerror="this.src='/images/product1.jpg'">
            </div>
            
            <div class="product-info-section">
                <h1 class="product-title"><%= product.name %></h1>
                <div class="product-category"><%= product.category %></div>
                
                <div class="product-price">
                                            <%= formatPrice(product.price) %>
                </div>
                
                <div class="product-quantity-info">
                    <div class="quantity-label">Tình trạng kho:</div>
                    <div class="quantity-value">
                        Số lượng: <%= product.quantity %>
                        <span class="quantity-status <%= product.quantity > 10 ? 'quantity-in-stock' : product.quantity > 0 ? 'quantity-low' : 'quantity-out' %>">
                            <%= product.quantity > 10 ? 'Còn hàng' : product.quantity > 0 ? 'Sắp hết' : 'Hết hàng' %>
                        </span>
                    </div>
                </div>
                
                <div class="product-description">
                    <%= product.description %>
                </div>
                
                <% if (product.features && product.features.length > 0) { %>
                    <div class="product-features">
                        <h3 class="features-title">Tính năng nổi bật:</h3>
                        <ul class="features-list">
                            <% product.features.forEach(function(feature) { %>
                                <li><%= feature %></li>
                            <% }) %>
                        </ul>
                    </div>
                <% } %>
                
                <div class="quantity-input">
                    <label for="quantity">Số lượng:</label>
                    <input type="number" id="quantity" name="quantity" value="1" min="1" max="<%= product.quantity %>" <%= product.quantity <= 0 ? 'disabled' : '' %>>
                </div>
                
                <div class="product-actions">
                    <button class="btn-buy-now" onclick="buyProduct()" <%= product.quantity <= 0 ? 'disabled' : '' %>>
                        <%= product.quantity <= 0 ? 'Hết hàng' : 'Mua ngay' %>
                    </button>
                    <a href="/products" class="btn-back">Quay lại</a>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('partials/footer') %>
    
    <script>
        function buyProduct() {
            // Kiểm tra xem user đã đăng nhập chưa
            var currentUser = JSON.parse(localStorage.getItem('currentUser') || '{}');
            if (!currentUser.id) {
                alert('Vui lòng đăng nhập để mua hàng!');
                window.location.href = '/login';
                return;
            }
            
            var quantity = parseInt(document.getElementById('quantity').value);
            var productId = <%- product.id %>;
            
            if (quantity <= 0) {
                alert('Vui lòng chọn số lượng hợp lệ');
                return;
            }
            
            if (quantity > <%- product.quantity %>) {
                alert('Số lượng vượt quá tồn kho');
                return;
            }
            
            if (confirm('Bạn có muốn mua ' + quantity + ' sản phẩm này không?')) {
                fetch('/products/api/decrease-quantity', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        productId: productId,
                        amount: quantity
                    })
                })
                .then(function(response) { 
                    if (response.status === 401) {
                        alert('Vui lòng đăng nhập để mua hàng!');
                        window.location.href = '/login';
                        throw new Error('Unauthorized');
                    }
                    if (!response.ok) {
                        throw new Error('Network response was not ok');
                    }
                    return response.json(); 
                })
                .then(function(data) {
                    if (data.success) {
                        alert('Mua hàng thành công! Đã mua ' + quantity + ' sản phẩm.');
                        window.location.href = '/products';
                    } else {
                        alert('Lỗi: ' + (data.message || 'Không xác định'));
                    }
                })
                .catch(function(error) {
                    if (error.message === 'Unauthorized') {
                        return; // Đã xử lý redirect
                    }
                    console.error('Error:', error);
                    alert('Có lỗi xảy ra khi mua hàng. Vui lòng thử lại.');
                });
            }
        }
        
        // Cập nhật max value của input quantity khi số lượng thay đổi
        document.getElementById('quantity').addEventListener('input', function() {
            var maxQuantity = <%- product.quantity %>;
            var currentValue = parseInt(this.value);
            
            if (currentValue > maxQuantity) {
                this.value = maxQuantity;
            }
            
            if (currentValue < 1) {
                this.value = 1;
            }
        });
    </script>
</body>
</html> 