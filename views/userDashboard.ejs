<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Lyn Salon</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .dashboard-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .dashboard-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            text-align: center;
        }

        .dashboard-header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .dashboard-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.3s;
        }

        .stat-card:hover {
            transform: translateY(-5px);
        }

        .stat-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: #007bff;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }

        .stat-label {
            color: #666;
            font-size: 1rem;
        }
        
        .dashboard-tabs {
            display: flex;
            background: white;
            border-radius: 15px;
            padding: 5px;
            margin-bottom: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            flex-wrap: wrap;
        }
        
        .tab-button {
            flex: 1;
            padding: 15px 20px;
            border: none;
            background: transparent;
            cursor: pointer;
            border-radius: 10px;
            font-weight: 500;
            transition: all 0.3s;
            min-width: 150px;
        }
        
        .tab-button.active {
            background: #007bff;
            color: white;
        }
        
        .tab-content {
            display: none;
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .tab-content.active {
            display: block;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
        }

        .btn-primary {
            background: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: background 0.3s;
        }
        
        .btn-primary:hover {
            background: #0056b3;
        }
        
        .appointment-card {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 4px solid #007bff;
            transition: transform 0.3s;
        }
        
        .appointment-card:hover {
            transform: translateY(-2px);
        }
        
        .appointment-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            flex-wrap: wrap;
            gap: 10px;
        }
        
        .service-name {
            font-weight: bold;
            color: #333;
            font-size: 1.1rem;
        }
        
        .status-badge {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
        }
        
        .status-pending {
            background: #fff3cd;
            color: #856404;
        }
        
        .status-confirmed {
            background: #d4edda;
            color: #155724;
        }
        
        .status-completed {
            background: #cce5ff;
            color: #004085;
        }
        
        .status-cancelled {
            background: #f8d7da;
            color: #721c24;
        }

        .appointment-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 15px;
        }
        
        .detail-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .detail-item i {
            color: #007bff;
            width: 20px;
        }

        .appointment-actions {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        
        .btn-action {
            padding: 8px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9rem;
            transition: background 0.3s;
        }

        .btn-cancel {
            background: #dc3545;
            color: white;
        }
        
        .btn-cancel:hover {
            background: #c82333;
        }
        
        .btn-reschedule {
            background: #ffc107;
            color: #212529;
        }
        
        .btn-reschedule:hover {
            background: #e0a800;
        }
        
        .btn-edit {
            background: #17a2b8;
            color: white;
        }
        
        .btn-edit:hover {
            background: #138496;
        }
        
        .cart-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px;
            border-bottom: 1px solid #eee;
            background: #f8f9fa;
            border-radius: 8px;
            margin-bottom: 10px;
        }
        
        .cart-item:last-child {
            border-bottom: none;
        }
        
        .cart-item-image {
            width: 60px;
            height: 60px;
            object-fit: cover;
            border-radius: 8px;
        }
        
        .cart-item-info {
            flex: 1;
        }
        
        .cart-item-name {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .cart-item-price {
            color: #e74c3c;
            font-weight: bold;
        }
        
        .cart-item-quantity {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .quantity-btn {
            width: 30px;
            height: 30px;
            border: 1px solid #ddd;
            background: white;
            border-radius: 5px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .quantity-btn:hover {
            background: #f8f9fa;
        }
        
        .cart-total {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
            text-align: right;
        }
        
        .total-amount {
            font-size: 1.5rem;
            font-weight: bold;
            color: #e74c3c;
        }
        
        .btn-checkout {
            background: #28a745;
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 15px;
        }
        
        .btn-checkout:hover {
            background: #218838;
        }
        
        .profile-section {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 30px;
        }
        
        .profile-avatar {
            text-align: center;
        }
        
        .avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: #007bff;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
            margin: 0 auto 20px;
        }
        
        .profile-info {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
        }
        
        .info-group {
            margin-bottom: 15px;
        }

        .info-label {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .info-value {
            color: #666;
        }
        
        .btn-edit-profile {
            background: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 15px;
        }
        
        .btn-edit-profile:hover {
            background: #0056b3;
        }
        
        .no-data {
            text-align: center;
            padding: 50px;
            color: #666;
        }
        
        .no-data i {
            font-size: 3rem;
            color: #ddd;
            margin-bottom: 20px;
        }

        .order-history {
            margin-top: 30px;
        }
        
        .order-card {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            border-left: 4px solid #28a745;
        }
        
        .order-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        
        .order-id {
            font-weight: bold;
            color: #333;
        }
        
        .order-date {
            color: #666;
            font-size: 0.9rem;
        }
        
        .order-items {
            margin-bottom: 15px;
        }
        
        .order-item {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
            border-bottom: 1px solid #eee;
        }
        
        .order-item:last-child {
            border-bottom: none;
        }
        
        .order-total {
            font-weight: bold;
            color: #e74c3c;
            text-align: right;
        }
        
        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }
        
        .quick-action-card {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
            transition: transform 0.3s;
            cursor: pointer;
        }
        
        .quick-action-card:hover {
            transform: translateY(-3px);
        }
        
        .quick-action-icon {
            font-size: 2rem;
            color: #007bff;
            margin-bottom: 10px;
        }
        
        .quick-action-title {
            font-weight: bold;
            color: #333;
            margin-bottom: 5px;
        }
        
        .quick-action-desc {
            color: #666;
            font-size: 0.9rem;
        }
        
        @media (max-width: 768px) {
            .dashboard-stats {
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            }
            
            .dashboard-tabs {
                flex-direction: column;
            }
            
            .profile-section {
                grid-template-columns: 1fr;
            }
            
            .appointment-details {
                grid-template-columns: 1fr;
            }
            
            .quick-actions {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1>Xin chào, <%= user.name %>! 👋</h1>
            <p>Quản lý lịch hẹn, giỏ hàng và thông tin cá nhân của bạn</p>
            </div>

        <div class="dashboard-stats">
                <div class="stat-card">
                    <i class="fas fa-calendar-check stat-icon"></i>
                    <div class="stat-number"><%= appointments.length %></div>
                    <div class="stat-label">Tổng lịch hẹn</div>
                </div>
                <div class="stat-card">
                    <i class="fas fa-clock stat-icon"></i>
                    <div class="stat-number">
                        <%= appointments.filter(a => a.status === 'pending').length %>
                    </div>
                    <div class="stat-label">Đang chờ</div>
                </div>
                <div class="stat-card">
                    <i class="fas fa-check-circle stat-icon"></i>
                    <div class="stat-number">
                        <%= appointments.filter(a => a.status === 'confirmed').length %>
                    </div>
                    <div class="stat-label">Đã xác nhận</div>
                </div>
            <div class="stat-card">
                <i class="fas fa-shopping-cart stat-icon"></i>
                <div class="stat-number" id="cart-count">0</div>
                <div class="stat-label">Sản phẩm trong giỏ</div>
            </div>
            <div class="stat-card">
                <i class="fas fa-shopping-bag stat-icon"></i>
                <div class="stat-number" id="order-count">0</div>
                <div class="stat-label">Đơn hàng đã mua</div>
            </div>
                <div class="stat-card">
                    <i class="fas fa-star stat-icon"></i>
                <div class="stat-number">5.0</div>
                <div class="stat-label">Đánh giá trung bình</div>
            </div>
        </div>

        <div class="quick-actions">
            <div class="quick-action-card" onclick="showTab('appointments')">
                <i class="fas fa-calendar-plus quick-action-icon"></i>
                <div class="quick-action-title">Đặt lịch mới</div>
                <div class="quick-action-desc">Đặt lịch hẹn với stylist</div>
            </div>
            <div class="quick-action-card" onclick="showTab('cart')">
                <i class="fas fa-shopping-cart quick-action-icon"></i>
                <div class="quick-action-title">Giỏ hàng</div>
                <div class="quick-action-desc">Xem và quản lý giỏ hàng</div>
                    </div>
            <div class="quick-action-card" onclick="showTab('profile')">
                <i class="fas fa-user-edit quick-action-icon"></i>
                <div class="quick-action-title">Chỉnh sửa thông tin</div>
                <div class="quick-action-desc">Cập nhật thông tin cá nhân</div>
                </div>
            <div class="quick-action-card" onclick="showTab('orders')">
                <i class="fas fa-history quick-action-icon"></i>
                <div class="quick-action-title">Lịch sử mua hàng</div>
                <div class="quick-action-desc">Xem các đơn hàng đã mua</div>
            </div>
        </div>

        <div class="dashboard-tabs">
            <button class="tab-button active" onclick="showTab('appointments')">
                <i class="fas fa-calendar"></i> Lịch hẹn
            </button>
            <button class="tab-button" onclick="showTab('cart')">
                <i class="fas fa-shopping-cart"></i> Giỏ hàng
            </button>
            <button class="tab-button" onclick="showTab('orders')">
                <i class="fas fa-shopping-bag"></i> Đơn hàng
            </button>
            <button class="tab-button" onclick="showTab('profile')">
                <i class="fas fa-user"></i> Thông tin cá nhân
            </button>
            </div>

        <!-- Tab: Lịch hẹn -->
        <div id="appointments-tab" class="tab-content active">
                <div class="section-header">
                    <h2 class="section-title">Lịch hẹn của tôi</h2>
                <a href="/book-appointment" class="btn-primary">
                    <i class="fas fa-plus"></i> Đặt lịch mới
                    </a>
                </div>

            <% if (appointments && appointments.length > 0) { %>
                <% appointments.forEach(function(appointment) { %>
                        <div class="appointment-card">
                            <div class="appointment-header">
                                <span class="service-name"><%= appointment.service_name %></span>
                                <span class="status-badge status-<%= appointment.status %>">
                                    <% if (appointment.status === 'pending') { %>Đang chờ<% } %>
                                    <% if (appointment.status === 'confirmed') { %>Đã xác nhận<% } %>
                                    <% if (appointment.status === 'completed') { %>Đã hoàn thành<% } %>
                                    <% if (appointment.status === 'cancelled') { %>Đã hủy<% } %>
                                </span>
                            </div>
                                                    <div class="appointment-details">
                            <div class="detail-item">
                                <i class="fas fa-user"></i>
                                <span><strong>Stylist:</strong> <%= appointment.stylist_name %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-calendar"></i>
                                <span><strong>Ngày:</strong> <%= new Date(appointment.appointment_date).toLocaleDateString('vi-VN') %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-clock"></i>
                                <span><strong>Giờ:</strong> <%= appointment.appointment_time %></span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-money-bill"></i>
                                <span><strong>Giá:</strong> <%= (appointment.service_price || 0).toLocaleString('vi-VN') %> VNĐ</span>
                            </div>
                        </div>
                        </div>
                        <div class="appointment-actions">
                            <% if (appointment.status === 'pending' || appointment.status === 'confirmed') { %>
                                <button class="btn-action btn-cancel" onclick="cancelAppointment(<%= appointment.id %>)">
                                            <i class="fas fa-times"></i> Hủy lịch
                                        </button>
                                <button class="btn-action btn-reschedule" onclick="rescheduleAppointment(<%= appointment.id %>)">
                                    <i class="fas fa-edit"></i> Đổi lịch
                                </button>
                                <button class="btn-action btn-edit" onclick="editAppointment(<%= appointment.id %>)">
                                    <i class="fas fa-cog"></i> Chỉnh sửa
                                </button>
                            <% } %>
                            <% if (appointment.status === 'completed') { %>
                                <button class="btn-action btn-edit" onclick="rateAppointment(<%= appointment.id %>)">
                                    <i class="fas fa-star"></i> Đánh giá
                                </button>
                            <% } %>
                        </div>
                        </div>
                    <% }); %>
            <% } else { %>
                <div class="no-data">
                    <i class="fas fa-calendar-times"></i>
                    <h3>Chưa có lịch hẹn nào</h3>
                    <p>Hãy đặt lịch hẹn đầu tiên của bạn để trải nghiệm dịch vụ tuyệt vời tại LYN Salon!</p>
                    <a href="/book-appointment" class="btn-primary">
                        <i class="fas fa-calendar-plus"></i> Đặt lịch ngay
                    </a>
                </div>
                <% } %>
        </div>

        <!-- Tab: Giỏ hàng -->
        <div id="cart-tab" class="tab-content">
            <div class="section-header">
                <h2 class="section-title">Giỏ hàng của tôi</h2>
                <a href="/products" class="btn-primary">
                    <i class="fas fa-shopping-bag"></i> Mua sắm thêm
                </a>
            </div>
            <div id="cart-items">
                <div class="no-data">
                    <i class="fas fa-shopping-cart"></i>
                    <h3>Giỏ hàng trống</h3>
                    <p>Hãy thêm sản phẩm vào giỏ hàng để bắt đầu mua sắm!</p>
                    <a href="/products" class="btn-primary">
                        <i class="fas fa-shopping-bag"></i> Mua sắm ngay
                    </a>
                </div>
            </div>
            <div id="cart-total" style="display: none;">
                <div class="cart-total">
                    <div class="total-amount">Tổng: <span id="total-amount">0</span> VNĐ</div>
                    <button class="btn-checkout" onclick="checkout()">
                        <i class="fas fa-credit-card"></i> Thanh toán
                    </button>
                </div>
            </div>
        </div>

        <!-- Tab: Đơn hàng -->
        <div id="orders-tab" class="tab-content">
            <div class="section-header">
                <h2 class="section-title">Lịch sử đơn hàng</h2>
            </div>
            <div class="order-history">
                <div class="no-data">
                    <i class="fas fa-shopping-bag"></i>
                    <h3>Chưa có đơn hàng nào</h3>
                    <p>Bạn chưa có đơn hàng nào. Hãy mua sắm để tạo đơn hàng đầu tiên!</p>
                    <a href="/products" class="btn-primary">
                        <i class="fas fa-shopping-bag"></i> Mua sắm ngay
                    </a>
                </div>
            </div>
        </div>

        <!-- Tab: Thông tin cá nhân -->
        <div id="profile-tab" class="tab-content">
            <div class="section-header">
                <h2 class="section-title">Thông tin cá nhân</h2>
                <button class="btn-primary" onclick="editProfile()">
                    <i class="fas fa-edit"></i> Chỉnh sửa
                </button>
            </div>
            <div class="profile-section">
                <div class="profile-avatar">
                    <div class="avatar">
                        <%= user.name.charAt(0).toUpperCase() %>
                    </div>
                    <button class="btn-edit-profile" onclick="changeAvatar()">
                        <i class="fas fa-camera"></i> Đổi ảnh
                    </button>
                </div>
                <div class="profile-info">
                    <div class="info-group">
                        <div class="info-label">Họ tên</div>
                        <div class="info-value"><%= user.name %></div>
                    </div>
                    <div class="info-group">
                        <div class="info-label">Email</div>
                        <div class="info-value"><%= user.email %></div>
                    </div>
                    <div class="info-group">
                        <div class="info-label">Vai trò</div>
                        <div class="info-value"><%= user.role === 'admin' ? 'Quản trị viên' : 'Khách hàng' %></div>
                    </div>
                    <div class="info-group">
                        <div class="info-label">Ngày tham gia</div>
                        <div class="info-value">Tháng 12, 2024</div>
                    </div>
                    <div class="info-group">
                        <div class="info-label">Số lịch hẹn</div>
                        <div class="info-value"><%= appointments.length %> lịch hẹn</div>
                    </div>
                    <div class="info-group">
                        <div class="info-label">Đánh giá trung bình</div>
                        <div class="info-value">5.0 ⭐</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Tab functionality
        function showTab(tabName) {
            // Hide all tabs
            document.querySelectorAll('.tab-content').forEach(function(tab) {
                tab.classList.remove('active');
            });
            document.querySelectorAll('.tab-button').forEach(function(btn) {
                btn.classList.remove('active');
            });
            
            // Show selected tab
            var targetTab = document.getElementById(tabName + '-tab');
            if (targetTab) {
                targetTab.classList.add('active');
            }
            
            // Add active class to clicked button
            if (event && event.target && event.target.classList.contains('tab-button')) {
                event.target.classList.add('active');
            }
        }

        // Cart functionality
        var cart = JSON.parse(localStorage.getItem('cart')) || [];
        
        function updateCartDisplay() {
            var cartItems = document.getElementById('cart-items');
            var cartTotal = document.getElementById('cart-total');
            var cartCount = document.getElementById('cart-count');
            
            cartCount.textContent = cart.length;
            
            if (cart.length === 0) {
                cartItems.innerHTML = '<div class="no-data"><i class="fas fa-shopping-cart"></i><h3>Giỏ hàng trống</h3><p>Hãy thêm sản phẩm vào giỏ hàng để bắt đầu mua sắm!</p><a href="/products" class="btn-primary"><i class="fas fa-shopping-bag"></i> Mua sắm ngay</a></div>';
                cartTotal.style.display = 'none';
            } else {
                var total = 0;
                var cartHTML = '';
                cart.forEach(function(item) {
                    total += item.price * item.quantity;
                    cartHTML += '<div class="cart-item"><img src="' + item.image + '" alt="' + item.name + '" class="cart-item-image" onerror="this.src=\'/images/product1.jpg\'"><div class="cart-item-info"><div class="cart-item-name">' + item.name + '</div><div class="cart-item-price">' + item.price.toLocaleString('vi-VN') + ' VNĐ</div></div><div class="cart-item-quantity"><button class="quantity-btn" onclick="updateQuantity(' + item.id + ', -1)">-</button><span>' + item.quantity + '</span><button class="quantity-btn" onclick="updateQuantity(' + item.id + ', 1)">+</button></div><button class="btn-action btn-cancel" onclick="removeFromCart(' + item.id + ')"><i class="fas fa-trash"></i></button></div>';
                });
                cartItems.innerHTML = cartHTML;
                document.getElementById('total-amount').textContent = total.toLocaleString('vi-VN');
                cartTotal.style.display = 'block';
            }
        }

        function updateQuantity(productId, change) {
            var item = cart.find(function(item) { return item.id === productId; });
            if (item) {
                item.quantity += change;
                if (item.quantity <= 0) {
                    removeFromCart(productId);
                } else {
                    localStorage.setItem('cart', JSON.stringify(cart));
                    updateCartDisplay();
                }
            }
        }

        function removeFromCart(productId) {
            cart = cart.filter(function(item) { return item.id !== productId; });
            localStorage.setItem('cart', JSON.stringify(cart));
            updateCartDisplay();
        }

        function checkout() {
            if (cart.length === 0) {
                alert('Giỏ hàng trống!');
                return;
            }
            alert('Chức năng thanh toán sẽ được phát triển sau!');
        }

        function cancelAppointment(appointmentId) {
            if (confirm('Bạn có chắc muốn hủy lịch hẹn này?')) {
                fetch('/cancel-appointment/' + appointmentId, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    }
                })
                .then(function(response) { return response.json(); })
                .then(function(data) {
                    if (data.success) {
                        alert('Hủy lịch hẹn thành công!');
                        location.reload();
                    } else {
                        alert('Lỗi: ' + data.message);
                    }
                })
                .catch(function(error) {
                    console.error('Error:', error);
                    alert('Có lỗi xảy ra khi hủy lịch hẹn');
                });
            }
        }

        function rescheduleAppointment(appointmentId) {
            alert('Chức năng đổi lịch sẽ được phát triển sau!');
        }

        function editAppointment(appointmentId) {
            alert('Chức năng chỉnh sửa lịch hẹn sẽ được phát triển sau!');
        }

        function rateAppointment(appointmentId) {
            alert('Chức năng đánh giá sẽ được phát triển sau!');
        }

        function editProfile() {
            alert('Chức năng chỉnh sửa thông tin sẽ được phát triển sau!');
        }

        function changeAvatar() {
            alert('Chức năng đổi ảnh đại diện sẽ được phát triển sau!');
        }

        // Initialize cart display
        updateCartDisplay();
        
        // Check if there's a specific tab to show
        var tabToShow = localStorage.getItem('showTab');
        if (tabToShow) {
            showTab(tabToShow);
            localStorage.removeItem('showTab'); // Clear after showing
        }
        
        // Update header cart count
        if (typeof updateHeaderCartCount === 'function') {
            updateHeaderCartCount();
        }
    </script>
</body>
</html> 